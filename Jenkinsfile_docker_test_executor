pipeline {
    agent any
    triggers {
        pollSCM('*/1 * * * *')
    }
    stages {
        stage('build-docker-base') {
            steps {
                sh "docker build -t imiglane/ubuntu_ruby_executor:latest . -f Dockerfile_executor"
            }
        }
    }
}